# Sprint 02 Final Code Re-review

Status: **PASS**

Candidate: `ca49be416bb60e32c2451eb1755fff4dc29f53e3`

Parent/main baseline: `189e600256fabef4975777bd4a100affbb520253`

Supersedes rejected candidates: `cc86a508faa51b74e8abb5466ef37e46da9e8e89`, `190c78808c6ea79f440d6783c907eb43e08403f3`

Review date: 2026-07-21

## Decision

The final candidate resolves all prior findings: P2-1 through P2-7, P3-1 and P3-2, plus the automatic-Miss live-feedback follow-up. The final remediation aligns calibration reads with the integer result contract, proves full component completion from fractional stored input, and independently tests timestamp, integer-calibration, and duplicate-note schema failures.

No new lifecycle, timing, scoring, schema, storage, security/privacy, dependency, or CI-maintainability regression was found. There are no unresolved P0, P1, P2, or P3 findings. Final verdict: **PASS**.

## Finding dispositions

### P2-1 — Inclusive timing-window boundaries: RESOLVED

- Scoring normalizes the seconds delta to a stable six-decimal millisecond value before both expiration and hit-window comparisons (`apps/web/src/demo/scoring.ts:14-20`, `apps/web/src/demo/scoring.ts:43-62`, `apps/web/src/demo/scoring.ts:98-110`). Perfect/good thresholds are shared with the persisted-result contract (`apps/web/src/demo/scoring.ts:6-12`, `packages/chart-schema/src/index.ts:3-5`).
- Integrated tests cover exact negative and positive ±50/±120 ms chart-relative boundaries, immediately out-of-window values, and exact miss-expiry behavior (`apps/web/src/demo/scoring.test.ts:38-71`). The result schema validates recorded offsets and labels against the same normalized thresholds (`packages/chart-schema/src/index.ts:104-129`).

### P2-2 — Delayed miss collection selected the wrong live judgment: RESOLVED

- Live input feedback selects only a newly added record with a non-null input timestamp, excluding older automatic misses returned before the current hit (`apps/web/src/demo/DemoGame.tsx:183-204`).
- The regression drives a 2.0-second hit while two earlier misses are uncollected and requires `Perfect +0 ms`, score 1,000, and miss count 2 (`apps/web/src/demo/DemoGame.test.tsx:86-100`).

### P2-3 — Stale pause/resume completions overwrote a replacement session: RESOLVED

- Start, start failure, and natural completion are guarded by captured engine identity plus a session generation, and unmount invalidates the generation before disposal (`apps/web/src/demo/DemoGame.tsx:95-106`, `apps/web/src/demo/DemoGame.tsx:138-180`).
- Pause/resume require an explicit successful engine result, revalidate engine/session after awaits, restrict functional phase transitions, and ignore stale errors (`apps/web/src/demo/DemoGame.tsx:209-263`). The engine rechecks context/status after async boundaries and reports invalidated operations as false (`apps/web/src/demo/audioEngine.ts:143-172`).
- Deferred component regressions prove old pause/resume promises cannot overwrite a restarted countdown (`apps/web/src/demo/DemoGame.test.tsx:174-238`), and direct engine coverage verifies disposed transport no-ops (`apps/web/src/demo/audioEngine.test.ts:118-133`).

### P2-4 — Hidden-during-countdown runs continued unseen: RESOLVED

- Visibility handling covers countdown and playing, checks `document.hidden` immediately, and preserves the source phase across pause/resume (`apps/web/src/demo/DemoGame.tsx:224-256`, `apps/web/src/demo/DemoGame.tsx:308-321`).
- Component regressions cover visibility pause during both playing and scheduled countdown, including resume back to `Get ready` (`apps/web/src/demo/DemoGame.test.tsx:260-331`).

### P2-5 — Foreign/impossible persisted results were accepted: RESOLVED

- `noteJudgmentSchema` enforces hit/miss nullability, input/note/offset consistency, and the timing-implied label. `gameResultSchema` requires bounded integer calibration and rejects duplicate note indexes (`packages/chart-schema/src/index.ts:69-163`).
- The storage boundary requires current song/chart identity, a complete ordered chart match, exact note times, and a summary derived from validated judgments on reads and writes (`apps/web/src/demo/resultStore.ts:64-117`).
- Persistence tests reject foreign identity, impossible semantics, summary tampering, corrupt metadata, and invalid pre-save values (`apps/web/src/demo/resultStore.test.ts:28-91`). Shared-schema tests cover valid edge timing and invalid judgment combinations directly (`packages/chart-schema/src/index.test.ts:85-157`).

### P2-6 — A throwing storage getter crashed initial render: RESOLVED

- Browser storage acquisition is guarded and nullable; denied reads fall back without touching storage (`apps/web/src/demo/resultStore.ts:13-33`, `apps/web/src/demo/resultStore.ts:99-102`). `DemoGame` acquires storage once through that boundary before initializing calibration/results (`apps/web/src/demo/DemoGame.tsx:73-80`).
- Helper and component regressions cover a throwing `SecurityError`, successful rendering, and a recoverable save message (`apps/web/src/demo/resultStore.test.ts:93-103`, `apps/web/src/demo/DemoGame.test.tsx:333-355`).

### P3-1 — Shared result contracts lacked direct tests: RESOLVED

- The chart-schema suite directly imports and exercises result/judgment schemas plus shared timing constants (`packages/chart-schema/src/index.test.ts:3-12`).
- It covers a valid result at timing-window edges, impossible judgment timing combinations, timestamp format, integer calibration, and duplicate notes (`packages/chart-schema/src/index.test.ts:85-218`).

### Automatic-Miss live-feedback follow-up: RESOLVED

- The audio-clock interval updates scoring state and live feedback to `Miss` only when new expired judgments are added; unchanged intervals do not overwrite feedback (`apps/web/src/demo/DemoGame.tsx:279-306`).
- A component regression advances time past the first good window without input and requires both `Miss` feedback and miss count 1 (`apps/web/src/demo/DemoGame.test.tsx:102-120`). The delayed-hit regression confirms that a simultaneous real hit still owns feedback (`apps/web/src/demo/DemoGame.test.tsx:86-100`).

### P2-7 — Fractional calibration conflicted with integer result completion: RESOLVED

- `readCalibrationOffset` now accepts only bounded integers, matching `gameResultSchema`'s integer calibration contract; fractional, non-finite, out-of-range, denied, or absent values fall back to zero (`apps/web/src/demo/resultStore.ts:23-33`, `packages/chart-schema/src/index.ts:141-149`).
- The result-store regression places `"42.4"` directly under the calibration key and requires the reader to return zero (`apps/web/src/demo/resultStore.test.ts:23-26`). This tests the stored-input boundary rather than the save helper's rounding behavior.
- The component regression starts from that same fractional stored value, requires visible `+0 ms`, starts the session, invokes its installed natural-completion callback, and requires `Track complete` (`apps/web/src/demo/DemoGame.test.tsx:134-153`). A schema-incompatible stored value can no longer reach `createGameResult` or strand the disposed run.

### P3-2 — Independent schema invariants shared one negative payload: RESOLVED

- Invalid timestamp and fractional calibration now run as separate table cases against an otherwise valid result fixture (`packages/chart-schema/src/index.test.ts:159-182`).
- Duplicate-note rejection has its own otherwise valid payload and assertion (`packages/chart-schema/src/index.test.ts:184-218`). Each validator can now regress only by failing its corresponding test.

## Current findings

### P0

None.

### P1

None.

### P2

None.

### P3

None.

## Architecture, security, privacy, and CI assessment

- Gameplay timing remains audio-clock based. Stable scoring normalization and persisted-record validation share the same timing thresholds, while Phaser remains a rendering consumer rather than an independent scoring clock.
- Calibration and results remain browser-local. There is no upload, telemetry, selected-file, raw-audio persistence, credential, or server-side data path. Denied and invalid local metadata now degrade to safe defaults or validated removal rather than breaking render/completion.
- The final remediation changes one reader predicate plus tests. `DemoGame.tsx`, scoring, audio-engine, and schema implementation are unchanged from the previous reviewed remediation candidate, so the established lifecycle and scoring dispositions remain intact.
- No dependency or lockfile change is present relative to main. The locked-license policy passes for 272 packages, and the bundled audio remains reproducible.
- CI retains read-only repository permissions and pinned action SHAs (`.github/workflows/ci.yml:11-28`). Sprint 02 artifact naming, paths, and summary labels are internally consistent (`.github/workflows/ci.yml:47-62`).
- GitHub Verify run `29786997949` for the exact final candidate completed successfully. Only run-level status was queried; no browser log, report, screenshot, video, manifest, or artifact was opened.

## Verification performed

- Confirmed `189e600256fabef4975777bd4a100affbb520253` is the final candidate's parent and merge base.
- Compared the full candidate against main and the final targeted delta against rejected candidate `190c78808c6ea79f440d6783c907eb43e08403f3`. Reconfirmed all earlier application, scoring, lifecycle, schema, storage, unit/component/E2E-source, package, and CI-source dispositions within the permitted isolation boundary.
- Confirmed the only final implementation change from `190c788...` is the calibration reader predicate; other final-delta changes are focused unit/component/schema regressions and review/story documentation.
- `npm test` — passed: 12 chart-schema tests and 50 web tests across 8 test files.
- `npm run lint` — passed.
- `npm run typecheck` — passed.
- `npm run build` — passed; Vite emitted the expected large-Phaser-chunk advisory.
- `npm run check:licenses` — passed for 272 locked packages.
- `npm run check:demo-audio` — passed byte-for-byte reproducibility.
- Candidate-to-main diff whitespace validation passed.
- Repository-wide `npm run verify`, formatting, secret scanning, dependency audit, and all browser commands were not run because the worktree contains prohibited/unrelated product-evidence material and browser execution is outside the isolation contract. Relevant permitted checks were run separately as listed above.

## Isolation attestation

I did not open, read, hash, render, or inspect the contents of any file under `story/02-fair-scoring-calibration/evidence/`. I did not open or interact with the application, production preview, localhost preview, or any website. I did not read or edit `story/02-fair-scoring-calibration/PRODUCT_REVIEW.md`, and I did not use Product Reviewer observations. I did not inspect screenshots, videos, visual manifests/artifacts, Playwright reports, browser-test logs/output, or CI artifacts, and I did not execute browser/E2E tests. Review of browser/E2E and evidence-capture source was source-only.
