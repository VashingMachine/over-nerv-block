# Sprint 07 final isolated code re-review

Status: **PASS**

Candidate: `bd23829fe1f1831e97d19c6f87cee4e59afb0f52`

Baseline: `cdd85eb54e502bef27bd1ec34a4926194fe2440a`

The candidate is one atomic commit directly atop the supplied baseline. No unresolved P0, P1, or P2 finding remains. Application source, schemas, workflow, and test source are byte-identical to the previously reviewed implementation; the final remediation corrects the evidence-capture preparation and assertions without changing product behavior.

## Findings

No blocking findings.

### P3 — storage migration and cleanup are performed during render

The unchanged editor still calls storage-reading, migration, and removal logic from a render-time `useMemo` (`apps/web/src/correction/RhythmCorrectionEditor.tsx:67-104`). React Strict Mode development double rendering can suppress the one-time `migrated` or `discarded` status on the second calculation. Stored corrections and production behavior remain correct, so this is nonblocking. Moving writes/removals behind initialization or an effect, with a Strict Mode component test, would make the lifecycle exact.

## Resolved findings

### Resolved — different-analysis isolation now has a real coexisting source document

After the foreign-document recovery, the script creates a fresh valid revision-one correction for the original analysis and records its exact key and serialized value (`scripts/capture-story-07-evidence.mjs:629-638`). It selects the different owned analysis, waits for its empty-correction state, and proves the original key/value remains the sole unchanged entry before editing (`capture-story-07-evidence.mjs:640-665`). One valid edit takes the second editor to revision one, and subsequent edits create a second per-fingerprint document. The script then requires exactly two documents and requires the original serialized value to remain byte-identical (`capture-story-07-evidence.mjs:667-713`).

Returning to the original owned analysis must visibly restore the saved correction at unchanged revision one, after which the privacy assertion requires exactly two compact correction documents (`capture-story-07-evidence.mjs:715-729`). This resolves the former P2: the different-analysis artifact and video description now reflect an actually prepared isolation state rather than empty storage.

### Resolved — minimum-beats rejection now proves no mutation

The script derives the exact expected revision from the initial beat count, asserts that revision before the rejected removal, waits for the minimum-beats error, and asserts the identical revision/history afterward (`scripts/capture-story-07-evidence.mjs:671-697`). The manifest's `minimum_beats` no-mutation claim is now supported.

### Resolved — inventory and manifest match the asserted isolation journey

The new return-to-original artifact is part of the exact inventory guard (`scripts/capture-story-07-evidence.mjs:802-836`). Manifest claims are limited to the exercised facts: different-analysis isolation, exactly two coexisting documents, and restoration of the original after returning (`capture-story-07-evidence.mjs:839-875`).

## Application assessment

- Strict shared schemas version and discriminate the bounded operation log, require revision/history agreement, and enforce corrected timing and meter invariants (`packages/chart-schema/src/index.ts:429-616`).
- Projection replays offset, half/double tempo, meter, downbeat, tap-grid, add, and remove operations deterministically from a parsed, cloned source without mutating the original (`apps/web/src/correction/rhythmCorrection.ts:66-426`). Undo and reset replay or clear that history.
- Storage is operations-only, per source fingerprint, and handles current, v0, corrupt, foreign, unavailable, and empty states (`apps/web/src/correction/correctionStore.ts:13-115`).
- The upload flow remounts the editor by source fingerprint. Corrected generation carries provenance into chart identity and the keyed gameplay instance (`apps/web/src/localAudio/LocalAudioPicker.tsx:914-920`; `apps/web/src/chartGeneration/generateDifficultyCharts.ts:184-285`; `apps/web/src/chartGeneration/GeneratedDifficultyPicker.tsx:188`).
- Unit/component coverage includes deterministic replay, source immutability, every operation family, invalid-edit nonmutation, schema invariants, fingerprint storage, v0 migration, corruption/denial, persistence/undo/reset, corrected provenance, changed IDs, and corrected note timing.

## Capture audit

The capture remains linear, bounded, and readable. It requires an exact build identifier, uses repository-owned synthesized inputs with private sentinel filenames, rejects unexpected external traffic and application writes, constrains output to the exact story directory or OS temporary storage, stages output atomically, hashes each artifact, and checks an explicit inventory before publishing. Invalid offset/tap/duplicate/tempo/minimum-beat states, pause stability, resume/restart/completion, repeated undo, reset, reload, matching restore, v0 migration, corrupt/foreign recovery, storage denial, two-fingerprint coexistence, return-to-original restoration, privacy, and narrow layout all have direct source assertions. Storage denial is isolated in a separate context and expects zero stored documents. No server deployment is required; the workflow remains loopback-only.

## Verification

- Git identity: candidate `bd23829fe1f1831e97d19c6f87cee4e59afb0f52` has baseline `cdd85eb54e502bef27bd1ec34a4926194fe2440a` as its sole parent and merge base. Baseline-to-candidate whitespace validation passes.
- Diff identity: relative to both prior candidates, application source, shared packages, tests, workflow, dependency manifests, and lockfile are unchanged. Final remediation is confined to capture source and non-code story summaries.
- Supplied exact-candidate CI run `29802543942`: **PASS**. Supplied artifact identity `8484302524`, digest `sha256:69ff94b6bf3b157ed56c3c49bd49ac7dbc4a9be370ad4f023474c4142ccf78ac`. Artifact contents were not accessed.
- Local non-browser checks pass: capture-script syntax, formatting and lint; repository lint and typecheck; production build; dependency audit with zero vulnerabilities; all 45 shared-schema tests and all 199 web unit/component tests.
- The production build retains the pre-existing Phaser chunk-size warning; it does not affect correctness or the static local-only model.

## Isolation attestation

The reviewer inspected only source code, unit/component/E2E and capture-script source, schemas, configuration/workflow source, dependency manifests, candidate/baseline/prior-candidate Git identity and diff, the prior isolated code report, and the supplied CI/artifact identity and conclusion. The reviewer ran only non-browser syntax/static checks, Node/jsdom tests, dependency audit, and production compilation.

The reviewer did **not** open, serve, or operate the site; run browser, E2E, or capture commands; inspect browser output, reports, or traces; inspect `story/07-correct-imperfect-chart/evidence/**` or any manifest, media, screenshot, or video; inspect the changed story summaries; or inspect `PRODUCT_REVIEW.md`, `CODE_REVIEW_STATUS`, or any Product Review output.

## Verdict

**PASS** — the former P2 is resolved and no unresolved P0-P2 finding remains.
